import { getDatabase, ref, set, onValue, child, get, push , update } from "firebase/database";

const database = getDatabase();

/* adds the new task that the user implements */
function addTask (){
    return null;
}


/* removes the specific task the user wishes to remove */
function deleteTask(){
    return null
}

export {};